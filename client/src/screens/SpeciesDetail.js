import { View, Text, Stylesheet } from 'react-native'
import StyledText from '../components/StyledText'

const SpeciesDetail = () => {
  return <StyledText>This is the species info page</StyledText>
}

export default SpeciesDetail
